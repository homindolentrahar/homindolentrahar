---
import ConnectWithMe from "@/components/home/ConnectWithMe";
import { Button } from "@/components/ui/button";
import { Download } from "iconoir-react";
---

<div class="grid grid-cols-1 lg:grid-cols-2 items-start gap-8 lg:gap-16">
  <div class="flex flex-col gap-4">
    <h1
      class="text-3xl lg:text-4xl text-gray-950 dark:text-white font-semibold text-center lg:text-start"
    >
      There is no such thing as perfection, there is only better iteration
    </h1>
    <ConnectWithMe
      client:load
      className="w-full justify-center lg:justify-start"
    />
  </div>
  <div class="flex flex-col gap-4">
    <p class="text-gray-600 dark:text-gray-300 text-center lg:text-start">
      I'm a detail oriented Mobile Developer with a passion for building
      beautiful, robust, and accessible apps. I also do a bit of web dev on
      frontend and backend side, along with UI/UX design sometimes.
    </p>
    <Button className="w-full lg:w-5/12">
      <Download />
      Download Resume</Button
    >
  </div>
</div>
